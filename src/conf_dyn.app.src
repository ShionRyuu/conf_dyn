{application, conf_dyn, [
    {description, "Dynamic Config Generator"},
    {vsn, "0.0.1"},
	{env, [
        {root_dir, "."},
        {conf_dir, "config"},
        {beam_dir, "ebin"}
    ]},
    {applications, [kernel, stdlib]},
    {registered, [conf_dyn]}
]}.
